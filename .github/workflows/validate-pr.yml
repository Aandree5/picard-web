name: Validate Pull Request

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: validate-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  pre-commit-check:
    name: Pre-commit Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.x"

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit hooks
        run: pre-commit run --all-files

  docker-build-healthcheck:
    name: Docker Build & Healthcheck (${{ matrix.variant }})
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    strategy:
      matrix:
        variant: [minimal, full]
    steps:
      - uses: actions/checkout@v7

      - name: Set up QEMU (for emulation)
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image
        run: |
          docker buildx build --load \
            --cache-from type=gha,scope=amd64 \
            --cache-to type=gha,mode=max,scope=amd64 \
            --target ${{ matrix.variant }} \
            -t picard-web:${{ matrix.variant }} .

      - name: Run container
        run: docker run -d --name picard-web-test-${{ matrix.variant }} picard-web:${{ matrix.variant }}

      - name: Wait for healthcheck to pass
        run: |
          for i in {1..10}; do
            STATUS=$(docker inspect --format='{{json .State.Health.Status}}' picard-web-test-${{ matrix.variant }})
            echo "Health status: $STATUS"
            if [ "$STATUS" == "\"healthy\"" ]; then
              echo "Container is healthy."
              exit 0
            fi
            sleep 5
          done
          echo "Container did not become healthy in time."
          docker logs picard-web-test-${{ matrix.variant }}
          exit 1

      - name: Build ARM64 Docker image (emulated)
        run: |
          docker buildx build --platform linux/arm64 --load \
            --cache-from type=gha,scope=arm64 \
            --cache-to type=gha,mode=max,scope=arm64 \
            --target ${{ matrix.variant }} \
            -t picard-web:${{ matrix.variant }}-arm .

      - name: Run ARM64 container (emulated)
        run: docker run -d --platform linux/arm64 --name picard-web-test-${{ matrix.variant }}-arm picard-web:${{ matrix.variant }}-arm

      - name: Wait for ARM64 healthcheck to pass
        run: |
          for i in {1..10}; do
            STATUS=$(docker inspect --format='{{json .State.Health.Status}}' picard-web-test-${{ matrix.variant }}-arm)
            echo "ARM health status: $STATUS"
            if [ "$STATUS" == "\"healthy\"" ]; then
              echo "ARM container is healthy."
              exit 0
            fi
            sleep 5
          done
          echo "ARM container did not become healthy in time."
          docker logs picard-web-test-${{ matrix.variant }}-arm
          exit 1
